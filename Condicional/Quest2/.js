let nome = prompt('nome')
let sexo = prompt('F ou M?') 
let estadoCivil = prompt('Informe seu estado civil')

if (sexo == 'F' && estadoCivil == 'Casada') {

    prompt('Informe o tempo de casada')
}

// Na linha 1, 2 e 3 foi declarado as variáveis;
// OBS é necessário que a variável 'sexo' seja declarado com a letra maiuscula;
// Na linha 5 foi dada a condição;
// Na linha 7 é dado o retorno se a condição for atendida. 